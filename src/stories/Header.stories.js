import React from "react";
import Header from "../components/Header";

export default {
  title: "Header"
};

export const HeaderDefault = () => <Header />;
export const HeaderMenuSelected = () => <Header selected />;
